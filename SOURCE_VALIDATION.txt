Archive Scout 3.0.0-beta.1.3 source validation

Completed locally before packaging:
- 90 unit, migration, and integration tests passed
- one display-dependent interface test was skipped in the normal headless run and passed separately under Xvfb
- the complete GUI opened, refreshed the Dashboard, and closed successfully under Xvfb
- python -m compileall completed for archive_scout, tests, and scripts
- both GitHub workflow files parsed as YAML
- Linux and macOS build scripts passed bash syntax validation
- dashboard counters reflected committed database changes without reopening or migrating the project
- dashboard reads used SQLite read-only mode, query_only, and a short busy timeout
- Windows automatic transport exposed httpx and urllib3 but not curl
- explicit curl mode was rejected under simulated Windows conditions
- Linux and macOS build scripts explicitly include the dynamically loaded curl fallback
- Windows build remained onedir and explicitly disabled UPX
- stable Windows version-resource metadata was added
- distributed Windows package staging no longer included the PowerShell/command installers
- tagged release workflow required Artifact Signing before publication
- signature verification occurred before final ZIP and hash generation
- SPDX 2.3 SBOM generation passed
- SHA-256 package-manifest generation was covered by source and packaging tests

Still required or unavailable locally:
- PowerShell was unavailable, so the three Windows PowerShell scripts were not executed or parsed by PowerShell in this container
- native Windows, Linux, and universal macOS packages were not built locally
- Azure Artifact Signing was not configured or exercised
- no Authenticode certificate was available for a real signature test
- Microsoft Defender and SmartScreen were not available for a real classification test
- the previously flagged binary still needs to be submitted to Microsoft as a clean false-positive sample
- GitHub Actions must run the complete cross-platform test and build workflows
- each packaged application should be smoke-tested on real hardware
