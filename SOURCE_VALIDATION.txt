Archive Scout 3.0.0-beta.1.5 source validation

Completed locally before packaging:
- 97 unit, migration, integration, packaging, interface, and workflow regression tests passed under Xvfb
- python -m compileall completed for archive_scout and tests
- both GitHub workflow files parsed as YAML
- Linux and macOS build scripts passed bash syntax validation
- Beta 1.4 application behavior and database schema were retained
- the shared CDX request-start interval remained fixed at 0.75 seconds (80 starts per minute)
- the default CDX page-worker count increased from 6 to 10
- the default numbered-page block size increased from 6 to 9
- the default resume-key request size increased from 25,000 to 50,000 rows
- HTTPX pooled keep-alive retention increased from 45 to 90 seconds
- the resume-response byte budget now scales with the 50,000-row request size
- untouched Beta 1.4 defaults migrate to the new values while custom network settings remain unchanged
- paged and resume-key parameter tests confirmed pageSize=9 and limit=50000
- existing timeout, 429, malformed-CDX, media, analysis, merge, backup, repair, icon, date, signing, external-media, and macOS packaging tests remained passing

Still required or not available locally:
- no live Internet Archive stress test was performed
- no real Windows, Linux, or universal macOS package was built in this container
- no native Windows Defender or SmartScreen scan was possible
- PowerShell was unavailable, so the Windows build script was not executed locally
- GitHub Actions must run the six-job test matrix and all three native package jobs
- each generated package should be smoke-tested on real hardware
