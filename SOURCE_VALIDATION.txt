Archive Scout 3.0.0-beta.1.1 source validation

Completed locally before packaging:
- 75 unit, migration, and integration tests passed
- python -m compileall completed for archive_scout and tests
- malformed valid-looking CDX JSON was converted into a splittable transient response
- automatic JSON-to-uncompressed-text fallback parsed CDX rows, resume keys, literal-space URLs, and page counts
- the corrected combined media filter used one `original:regex` expression for every selected extension
- explicit prefix media targets were normalized without a literal wildcard
- legacy `.jpg&ref=...` and query-value media extensions were recognized
- extensionless Flash MIME captures were classified as video media
- all previous schema, transport, paging, retry, analysis, merge, backup, repair, and interface tests remained passing
- both GitHub workflow YAML files parsed successfully
- Linux and macOS build scripts passed shell syntax validation

Not completed locally:
- no live Internet Archive stress test was performed
- the exact user response body that failed at line 3742 was not available in full
- no real corporate proxy, antivirus interception, or restrictive Windows firewall environment was available
- native Windows, Linux, and universal macOS packages were not built in this container

Still required on GitHub and real machines:
- Tests workflow on Windows, Linux, and macOS
- Build All Platforms workflow
- smoke test each packaged application
- rerun a live index that previously produced malformed CDX JSON
- run a corrected combined media index and verify media downloads
- test at least one Windows network that previously produced repeated CDX contact failures
